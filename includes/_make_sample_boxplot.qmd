### `r genes_no_bb[[i]]`

```{r}
#| echo: false
if (i <= length(genes_no_bb) && !is.null(genes_no_bb[[i]])) {
  make_sample_plot(bed_annotated, group_by_gene=TRUE, gene=genes_no_bb[[i]])
} else {
  cat("No data available for index", i)
}
```
